---
description: 数据库集合文档中，新加集合的索引建议必须标明升序或降序
globs: **/数据库集合文档.md
alwaysApply: false
---

# 数据库文档 - 索引建议规范

在《数据库集合文档》中**新增或修改集合**时，**索引建议**必须明确写出每个字段的排序方向：

- **升序**：`asc` 或「升序」（常用于时间正序、ID 正序、列表按创建时间从旧到新）
- **降序**：`desc` 或「降序」（常用于时间倒序、列表按创建时间从新到旧）

## 示例

**正确示例：**

- `createdAt` 升序 + `status` 升序
- `storeId` 升序 + `createdAt` 降序（按店铺、再按时间倒序）
- `merchantId` 升序 + `status` 升序 + `createdAt` 降序

**错误示例（缺少排序方向）：**

- `merchantId` + `status`（未标明升序/降序）
- `storeId` + `createdAt`（未标明升序/降序）

在文档中写「索引建议」时，可用文字说明（如「createdAt 降序」）或在表格/列表中注明 `asc`/`desc`，确保后续建索引时顺序明确。
