# 校园外卖系统 - 数据库集合文档

> 本文档详细说明每个数据库集合的用途、字段结构和数据类型  
> 最后更新：2026-04-06

---

## 📋 目录

- [用户相关集合](#用户相关集合)
- [商家相关集合](#商家相关集合)
- [订单相关集合](#订单相关集合)
- [商品相关集合](#商品相关集合)
- [店铺相关集合](#店铺相关集合)
- [内容管理集合](#内容管理集合)
- [系统管理集合](#系统管理集合)
- [其他集合](#其他集合)

---

## 📐 编写规范

- **索引建议**：凡在本文档中新增或修改集合时，**索引建议**必须标明每个字段的排序方向：
  - **升序**：`asc` / 升序（如按时间从旧到新）
  - **降序**：`desc` / 降序（如按时间从新到旧）
- 示例：`storeId` 升序 + `createdAt` 降序；`merchantId` 升序 + `status` 升序 + `createdAt` 降序。

---

## 用户相关集合

### 1. users - 用户信息表

**用途：** 存储小程序用户的基本信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 用户ID（自动生成） | `"user_123456"` |
| `openid` | String | 是 | 微信用户OpenID（唯一标识） | `"oABC123..."` |
| `unionid` | String | 否 | 微信UnionID | `"union_123..."` |
| `nickname` | String | 否 | 用户昵称 | `"张三"` |
| `avatar` | String | 否 | 用户头像URL | `"https://..."` |
| `phone` | String | 否 | 联系电话 | `"13800138000"` |
| `email` | String | 否 | 邮箱地址 | `"user@example.com"` |
| `campus` | String | 否 | 所在校区 | `"南校区"` |
| `college` | String | 否 | 所在学院 | `"计算机学院"` |
| `major` | String | 否 | 专业 | `"软件工程"` |
| `role` | String | 否 | 用户角色（user/merchant/admin） | `"user"` |
| `merchantName` | String | 否 | 商家名称（当role为merchant时） | `"我的店铺"` |
| `status` | String | 否 | 用户状态（active/banned） | `"active"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |
| `lastLoginAt` | Date | 否 | 最后登录时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `openid` - 唯一索引
- `status` + `createdAt` - 复合索引

---

### 2. addresses - 收货地址表

**用途：** 存储用户的收货地址信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 地址ID（自动生成） | `"addr_123456"` |
| `userId` | String | 是 | 用户ID（关联users._id） | `"user_123456"` |
| `userOpenid` | String | 是 | 用户OpenID（冗余字段，便于查询） | `"oABC123..."` |
| `name` | String | 是 | 收货人姓名 | `"张三"` |
| `phone` | String | 是 | 收货人电话 | `"13800138000"` |
| `addressDetail` | String | 是 | 详细地址 | `"XX路XX号XX栋XX室"` |
| `buildingName` | String | 否 | 楼栋名称 | `"1号宿舍楼"` |
| `houseNumber` | String | 否 | 门牌号 | `"101室"` |
| `isDefault` | Boolean | 否 | 是否默认地址 | `true` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `userId` + `isDefault` - 复合索引
- `userOpenid` - 索引

---

## 商家相关集合

### 3. merchants - 商家信息表

**用途：** 存储商家账号和基本信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 商家ID（自动生成） | `"merchant_123456"` |
| `openid` | String | 否 | 微信OpenID（微信登录商家） | `"oABC123..."` |
| `merchantName` | String | 是 | 商家名称 | `"美味餐厅"` |
| `account` | String | 是 | 登录账号（唯一） | `"merchant001"` |
| `password` | String | 是 | 登录密码（SHA256加密） | `"abc123..."`（加密后） |
| `contactPhone` | String | 否 | 联系电话 | `"13800138000"` |
| `avatar` | String | 否 | 商家头像URL | `"https://..."` |
| `storeId` | String | 否 | 关联的店铺ID（关联stores._id） | `"store_123456"` |
| `role` | String | 否 | 角色（owner/staff） | `"owner"` |
| `status` | String | 是 | 商家状态（pending/active/suspended/deleted） | `"active"` |
| `inviteCodeId` | String | 否 | 使用的邀请码ID | `"invite_123456"` |
| `contractStatus` | String | 否 | 合同状态（unsigned/signed） | `"signed"` |
| `qualificationStatus` | String | 否 | 资质审核状态（pending/approved/rejected） | `"approved"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `account` - 唯一索引
- `openid` - 索引
| `status` + `createdAt` - 复合索引

---

### 4. stores - 店铺信息表

**用途：** 存储店铺的详细信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 店铺ID（自动生成） | `"store_123456"` |
| `merchantId` | String | 是 | 商家ID（关联merchants._id） | `"merchant_123456"` |
| `name` | String | 是 | 店铺名称 | `"美味餐厅"` |
| `logoUrl` | String | 否 | 店铺Logo URL | `"https://..."` |
| `avatar` | String | 否 | 店铺头像（兼容字段） | `"https://..."` |
| `announcement` | String | 否 | 店铺公告 | `"欢迎光临！"` |
| `businessStatus` | String | 是 | 营业状态（open/closed） | `"open"` |
| `businessHours` | Object | 否 | 营业时间 | `{startTime: "09:00", endTime: "22:00"}` |
| `deliveryArea` | String | 否 | 配送范围 | `"校园内配送"` |
| `address` | String | 否 | 店铺地址 | `"XX路XX号"` |
| `storeCategory` | String | 否 | 店铺分类 | `"餐饮"` |
| `category` | String | 否 | 经营分类 | `"中餐"` |
| `minOrderAmount` | Number | 否 | 最低起送金额（元） | `20` |
| `deliveryFee` | Number | 否 | 配送费（元） | `3` |
| `autoAccept` | Boolean | 否 | 是否自动接单 | `false` |
| `ratingAvg` | Number | 否 | 平均评分 | `4.5` |
| `ratingCount` | Number | 否 | 评价数量 | `100` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `merchantId` - 索引
- `businessStatus` + `storeCategory` - 复合索引
- `ratingAvg` - 索引（用于排序）

---

## 订单相关集合

### 5. orders - 订单信息表

**用途：** 存储所有订单的详细信息（外卖订单、跑腿订单、代打订单等）

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 订单ID（自动生成） | `"order_123456"` |
| `orderNo` | String | 是 | 订单号（唯一） | `"ORD1234567890ABC"` |
| `userId` | String | 是 | 用户ID（关联users._id） | `"user_123456"` |
| `userOpenid` | String | 是 | 用户OpenID（冗余字段） | `"oABC123..."` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `storeName` | String | 是 | 店铺名称（冗余字段） | `"美味餐厅"` |
| `items` | Array | 是 | 订单商品列表 | `[{productId, productName, price, quantity, spec}]` |
| `amountGoods` | Number | 是 | 商品金额（分） | `5000`（50元） |
| `amountDelivery` | Number | 是 | 配送费（分，按楼层计费：1~3楼=150，4~6楼=200） | `150`（1.5元） |
| `platformFee` | Number | 是 | 平台服务费（分） | `400`（4元） |
| `platformFeeRate` | Number | 是 | 平台服务费比例 | `0.08`（8%） |
| `amountDiscount` | Number | 否 | 优惠金额（分） | `0` |
| `amountTotal` | Number | 是 | 订单总金额（分，当前与应付金额一致，仅配送费） | `150`（1.5元） |
| `amountPayable` | Number | 是 | 应付金额（分，仅配送费在线支付） | `150` |
| `merchantIncome` | Number | 是 | 商家线上收入（分；餐费改为线下转账后该值为0） | `0` |
| `deliveryFloor` | Number | 否 | 地址识别出的楼层（1~6，无法识别为0） | `3` |
| `riderIncome` | Number | 否 | 骑手结算收入（分，1~3楼=100，4~6楼=130） | `100` |
| `address` | Object | 是 | 配送地址信息 | `{name, phone, address, addressDetail}` |
| `remark` | String | 否 | 订单备注 | `"不要辣"` |
| `needCutlery` | Boolean | 否 | 是否需要餐具 | `true` |
| `cutleryQuantity` | Number | 否 | 餐具数量 | `2` |
| `deliveryType` | String | 是 | 配送方式（delivery/pickup） | `"delivery"` |
| `orderStatus` | String | 是 | 订单状态 | `"pending"` |
| `payStatus` | String | 是 | 支付状态（paid/unpaid） | `"paid"` |
| `expiredAt` | Date | 是 | 订单超时时间 | `2025-01-01T00:20:00Z` |
| `hasReview` | Boolean | 否 | 是否已评价 | `false` |
| `reviewId` | String | 否 | 评价ID（关联reviews._id） | `"review_123456"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |
| `unifiedPaymentId` | String | 否 | 统一付款单ID（关联 unified_payments._id，一次支付多单时使用） | `"xxx"` |

**订单状态说明：**
- `pending` - 待确认（仅历史数据或兼容；新外卖订单创建即为 confirmed）
- `confirmed` - 已确认（外卖订单创建时即为该状态，支付成功后直接进骑手大厅，无需商家确认；商家仅查看待做订单）
- `preparing` - 制作中
- `ready` - 待配送（商家已准备好）
- `delivering` - 配送中
- `completed` - 已完成
- `cancelled` - 已取消

**索引建议：**
- `orderNo` - 唯一索引
- `userId` + `createdAt` - 复合索引
- `storeId` + `orderStatus` + `createdAt` - 复合索引
- `orderStatus` + `createdAt` - 复合索引

---

### 5.1 unified_payments - 统一付款单

**用途：** 一次支付多店订单时，记录该笔支付对应的多笔订单，便于调起微信支付及支付成功后批量更新订单状态；平台统一收款，每笔订单仍归属对应商户。

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 自动生成 | - |
| `out_trade_no` | String | 是 | 微信支付商户订单号（唯一），用于统一下单 | `"U1730..."` |
| `userOpenid` | String | 是 | 下单用户 openid | `"oABC..."` |
| `orderIds` | Array | 是 | 本笔支付包含的订单 _id 列表 | `["id1","id2"]` |
| `totalFeeFen` | Number | 是 | 支付总金额（分） | `8000` |
| `status` | String | 是 | pending / paid | `"pending"` |
| `createdAt` | Date | 是 | 创建时间 | - |
| `updatedAt` | Date | 否 | 更新时间 | - |
| `paidAt` | Date | 否 | 支付成功时间 | - |

**索引建议：**
- `out_trade_no` - 唯一索引
- `userOpenid` + `status` - 复合索引

---

### 6. refunds - 退款申请表

**用途：** 存储订单退款申请和处理信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 退款ID（自动生成） | `"refund_123456"` |
| `refundNo` | String | 是 | 退款单号（唯一） | `"RF1234567890ABC"` |
| `orderId` | String | 是 | 订单ID（关联orders._id） | `"order_123456"` |
| `orderNo` | String | 是 | 订单号（冗余字段） | `"ORD1234567890ABC"` |
| `userId` | String | 是 | 用户ID（关联users._id） | `"user_123456"` |
| `userOpenid` | String | 是 | 用户OpenID | `"oABC123..."` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `selectedItems` | Array | 是 | 退款商品列表 | `[{productId, productName, quantity, spec}]` |
| `refundAmount` | Number | 是 | 退款金额（元） | `50.00` |
| `refundReason` | String | 是 | 退款原因 | `"商品质量问题"` |
| `refundReasonText` | String | 否 | 退款原因详细说明 | `"商品有异味"` |
| `images` | Array | 否 | 退款凭证图片 | `["https://..."]` |
| `refundSource` | String | 否 | 退款来源（user/merchant/admin） | `"user"` |
| `status` | String | 是 | 退款状态 | `"pending"` |
| `merchantReply` | String | 否 | 商家回复 | `"已处理"` |
| `adminReply` | String | 否 | 管理员回复 | `"同意退款"` |
| `processedAt` | Date | 否 | 处理时间 | `2025-01-01T00:00:00Z` |
| `processedBy` | String | 否 | 处理人（merchantId/adminId） | `"merchant_123456"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**退款状态说明：**
- `pending` - 待处理
- `processing` - 处理中
- `approved` - 已同意
- `rejected` - 已拒绝
- `completed` - 已完成（已退款）
- `cancelled` - 已取消

**索引建议：**
- `refundNo` - 唯一索引
- `orderId` + `status` - 复合索引
- `userId` + `createdAt` - 复合索引
- `storeId` + `status` - 复合索引

---

## 商品相关集合

### 7. products - 商品信息表

**用途：** 存储商家店铺的商品信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 商品ID（自动生成） | `"product_123456"` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `categoryId` | String | 是 | 分类ID（关联categories._id） | `"category_123456"` |
| `name` | String | 是 | 商品名称 | `"宫保鸡丁"` |
| `description` | String | 否 | 商品描述 | `"经典川菜"` |
| `coverUrl` | String | 否 | 商品封面图URL | `"https://..."` |
| `price` | Number | 是 | 商品价格（分） | `2500`（25元） |
| `originalPrice` | Number | 否 | 原价（分，用于显示折扣） | `3000`（30元） |
| `stock` | Number | 否 | 库存数量 | `100` |
| `sales` | Number | 否 | 销量 | `500` |
| `prepTime` | Number | 否 | 制作时间（分钟） | `15` |
| `spec` | String | 否 | 规格说明（旧格式） | `"大碗、微辣"` |
| `specifications` | Array | 否 | 规格配置（新格式） | `[{name: "大小", options: [...]}]` |
| `attr` | Array | 否 | 商品属性 | `[{name: "辣度", value: "微辣"}]` |
| `status` | String | 是 | 商品状态 | `"on"` |
| `auditStatus` | String | 是 | 审核状态 | `"approved"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**商品状态说明：**
- `on` - 上架
- `off` - 下架
- `deleted` - 已删除

**审核状态说明：**
- `pending` - 待审核
- `approved` - 已通过
- `rejected` - 已拒绝

**索引建议：**
- `storeId` + `status` - 复合索引
- `categoryId` + `status` - 复合索引
- `auditStatus` + `createdAt` - 复合索引

---

### 8. categories - 商品分类表

**用途：** 存储商家店铺的商品分类

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 分类ID（自动生成） | `"category_123456"` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `merchantId` | String | 是 | 商家ID（关联merchants._id） | `"merchant_123456"` |
| `name` | String | 是 | 分类名称 | `"热菜"` |
| `icon` | String | 否 | 分类图标URL | `"https://..."` |
| `sortOrder` | Number | 是 | 排序序号 | `1` |
| `status` | String | 是 | 分类状态（active/deleted） | `"active"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `storeId` + `status` + `sortOrder` - 复合索引

---

### 9. idle_products - 闲置商品表

**用途：** 存储用户发布的二手/闲置商品信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 商品ID（自动生成） | `"idle_123456"` |
| `title` | String | 是 | 商品标题 | `"二手iPhone 12"` |
| `description` | String | 否 | 商品描述 | `"九成新，功能正常"` |
| `price` | Number | 是 | 商品价格（元） | `2000` |
| `originalPrice` | Number | 否 | 原价（元） | `3000` |
| `category` | String | 是 | 商品分类 | `"电子产品"` |
| `location` | String | 否 | 所在位置 | `"南校区"` |
| `tags` | Array | 否 | 商品标签 | `["二手", "手机"]` |
| `images` | Array | 是 | 商品图片列表 | `["https://..."]` |
| `sellerId` | String | 是 | 卖家ID（用户OpenID） | `"oABC123..."` |
| `sellerName` | String | 是 | 卖家名称 | `"张三"` |
| `sellerAvatar` | String | 否 | 卖家头像 | `"https://..."` |
| `status` | String | 是 | 商品状态 | `"active"` |
| `viewCount` | Number | 否 | 浏览次数 | `100` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**商品状态说明：**
- `active` - 在售
- `sold` - 已售出
- `deleted` - 已删除

**索引建议：**
- `sellerId` + `status` - 复合索引
- `category` + `status` + `createdAt` - 复合索引
- `status` + `createdAt` - 复合索引

---

## 店铺相关集合

### 10. store_photos - 店铺照片表

**用途：** 存储店铺的环境照片、菜品照片等

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 照片ID（自动生成） | `"photo_123456"` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `merchantId` | String | 是 | 商家ID（关联merchants._id） | `"merchant_123456"` |
| `name` | String | 否 | 照片名称 | `"店铺环境1"` |
| `category` | String | 是 | 照片分类 | `"environment"` |
| `fileId` | String | 是 | 云存储文件ID | `"cloud://..."` |
| `url` | String | 是 | 照片URL | `"cloud://..."` |
| `status` | String | 是 | 照片状态 | `"active"` |
| `auditStatus` | String | 是 | 审核状态 | `"approved"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**照片分类说明：**
- `environment` - 环境照片
- `dish` - 菜品照片
- `license` - 证件照片（需要审核）

**审核状态说明：**
- `pending` - 待审核
- `approved` - 已通过
- `rejected` - 已拒绝

**索引建议：**
- `storeId` + `category` + `status` - 复合索引
- `auditStatus` + `createdAt` - 复合索引

---

## 内容管理集合

### 11. announcements - 公告表

**用途：** 存储系统公告和通知

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 公告ID（自动生成） | `"announce_123456"` |
| `title` | String | 是 | 公告标题 | `"系统维护通知"` |
| `content` | String | 是 | 公告内容 | `"系统将于..."` |
| `status` | String | 是 | 公告状态 | `"active"` |
| `priority` | Number | 否 | 优先级（数字越大优先级越高） | `10` |
| `targetType` | String | 是 | 发布范围 | `"all"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**发布范围说明：**
- `all` - 全部用户
- `client` - 客户端用户
- `merchant` - 商家用户

**索引建议：**
- `status` + `targetType` + `priority` - 复合索引

---

### 12. banners - 轮播图表

**用途：** 存储首页轮播图信息

**Web 管理端说明：** `admin-web` 使用 `@cloudbase/js-sdk` 的 `uploadFile` 上传至云存储（建议 `cloudPath` 前缀 `banners/`），将返回的 **fileID**（`cloud://` 开头）写入 `imageUrl` 字段，与小程序端 `wx.cloud.uploadFile` 一致。

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 轮播图ID（自动生成） | `"banner_123456"` |
| `imageUrl` | String | 是 | 图片地址：HTTPS URL 或云存储 **fileID**（`cloud://...`） | `"https://..."` / `"cloud://..."` |
| `linkUrl` | String | 否 | 跳转链接 | `"pages/store/index?id=123"` |
| `sortOrder` | Number | 是 | 排序序号 | `1` |
| `status` | String | 是 | 状态（active/inactive） | `"active"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `status` + `sortOrder` - 复合索引

---

### 13. reviews - 评价表

**用途：** 存储用户对订单/店铺的评价

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 评价ID（自动生成） | `"review_123456"` |
| `orderId` | String | 否 | 订单ID（关联orders._id） | `"order_123456"` |
| `userId` | String | 是 | 用户OpenID | `"oABC123..."` |
| `userName` | String | 是 | 用户名称 | `"张三"` |
| `userAvatar` | String | 否 | 用户头像 | `"https://..."` |
| `storeId` | String | 是 | 店铺ID（关联stores._id） | `"store_123456"` |
| `rating` | Number | 是 | 评分（1-5） | `5` |
| `content` | String | 否 | 评价内容 | `"很好吃！"` |
| `images` | Array | 否 | 评价图片 | `["https://..."]` |
| `merchantReply` | String | 否 | 商家回复 | `"感谢您的评价！"` |
| `hasMerchantReply` | Boolean | 否 | 是否有商家回复 | `true` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `storeId` + `createdAt` - 复合索引
- `userId` + `createdAt` - 复合索引
- `orderId` - 索引

---

### 14. chat_messages - 聊天消息表

**用途：** 存储用户之间的聊天消息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 消息ID（自动生成） | `"msg_123456"` |
| `chatId` | String | 是 | 会话ID（唯一标识两个用户的会话） | `"chat_abc123..."` |
| `fromUserId` | String | 是 | 发送者OpenID | `"oABC123..."` |
| `fromUserName` | String | 是 | 发送者名称 | `"张三"` |
| `fromUserAvatar` | String | 否 | 发送者头像 | `"https://..."` |
| `toUserId` | String | 是 | 接收者OpenID | `"oDEF456..."` |
| `toUserName` | String | 是 | 接收者名称 | `"李四"` |
| `toUserAvatar` | String | 否 | 接收者头像 | `"https://..."` |
| `content` | String | 是 | 消息内容 | `"你好"` |
| `messageType` | String | 是 | 消息类型 | `"order"` |
| `relatedId` | String | 否 | 关联ID（订单ID等） | `"order_123456"` |
| `relatedTitle` | String | 否 | 关联标题 | `"订单#123456"` |
| `status` | String | 是 | 消息状态 | `"unread"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**消息类型说明：**
- `order` - 订单相关
- `gaming` - 代打游戏相关
- `express` - 跑腿相关
- `reward` - 悬赏相关
- `secondhand` - 二手商品相关

**索引建议：**
- `chatId` + `createdAt` - 复合索引
- `fromUserId` + `createdAt` - 复合索引
- `toUserId` + `status` - 复合索引

---

### 15. chat_list - 聊天列表表

**用途：** 存储用户的聊天会话列表（用于快速显示最近聊天）

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 记录ID（自动生成） | `"chatlist_123456"` |
| `chatId` | String | 是 | 会话ID | `"chat_abc123..."` |
| `userId` | String | 是 | 用户OpenID | `"oABC123..."` |
| `otherUserId` | String | 是 | 对方用户OpenID | `"oDEF456..."` |
| `otherUserName` | String | 是 | 对方用户名称 | `"李四"` |
| `otherUserAvatar` | String | 否 | 对方用户头像 | `"https://..."` |
| `lastMessage` | String | 否 | 最后一条消息内容 | `"你好"` |
| `lastMessageType` | String | 否 | 最后一条消息类型 | `"order"` |
| `relatedId` | String | 否 | 关联ID | `"order_123456"` |
| `relatedTitle` | String | 否 | 关联标题 | `"订单#123456"` |
| `unreadCount` | Number | 否 | 未读消息数 | `5` |
| `updatedAt` | Date | 是 | 更新时间（最后消息时间） | `2025-01-01T00:00:00Z` |

**索引建议：**
- `userId` + `updatedAt` - 复合索引
- `chatId` - 索引

---

### 16. messages - 系统消息表

**用途：** 存储系统通知消息（订单通知、审核通知等）

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 消息ID（自动生成） | `"message_123456"` |
| `fromUserId` | String | 是 | 发送者OpenID | `"oABC123..."` |
| `fromUserName` | String | 是 | 发送者名称 | `"系统"` |
| `toUserId` | String | 是 | 接收者OpenID | `"oDEF456..."` |
| `toUserName` | String | 是 | 接收者名称 | `"李四"` |
| `messageType` | String | 是 | 消息类型 | `"order"` |
| `relatedId` | String | 否 | 关联ID | `"order_123456"` |
| `relatedTitle` | String | 否 | 关联标题 | `"订单#123456"` |
| `contactPhone` | String | 否 | 联系电话 | `"13800138000"` |
| `contactAction` | String | 否 | 联系操作（call/message） | `"call"` |
| `status` | String | 是 | 消息状态 | `"unread"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**消息状态说明：**
- `unread` - 未读
- `read` - 已读

**索引建议：**
- `toUserId` + `status` + `createdAt` - 复合索引
- `messageType` + `relatedId` - 复合索引

---

## 系统管理集合

### 17. admin_accounts - 管理员账号表

**用途：** 存储管理员账号信息

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 管理员ID（自动生成） | `"admin_123456"` |
| `username` | String | 是 | 登录账号（唯一） | `"admin"` |
| `password` | String | 是 | 登录密码（SHA256加密） | `"abc123..."`（加密后） |
| `role` | String | 是 | 角色（super_admin/admin） | `"super_admin"` |
| `permissions` | Array | 否 | 权限列表 | `["all"]` |
| `status` | String | 是 | 状态（active/inactive） | `"active"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `username` - 唯一索引
- `status` - 索引

---

### 17.1 admin_sessions - 管理员登录会话表

**用途：** 管理员通过 `adminManage.adminLogin` 登录后下发的服务端会话，供小程序管理端与 Web 管理后台在云函数中校验（字段 `adminSessionToken` 随云函数请求传递）。

**注意：** 请在云开发控制台 **数据库** 中 **手动创建** 本集合（名称 `admin_sessions`）；若不存在，登录写会话时会报 `-502005` 集合不存在。

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `token` | String | 是 | 随机会话令牌（建议建唯一索引） |
| `adminId` | String | 是 | 对应 `admin_accounts._id` |
| `expiresAt` | Date | 是 | 过期时间（默认约 24 小时） |
| `createdAt` | Date | 是 | 创建时间 |

**索引建议：**
- `token` - 唯一索引（查询校验）

---

### 18. admin_logs - 管理员操作日志表

**用途：** 记录管理员的操作日志

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 日志ID（自动生成） | `"log_123456"` |
| `adminId` | String | 是 | 管理员ID | `"admin_123456"` |
| `action` | String | 是 | 操作类型 | `"updateMerchantStatus"` |
| `target` | String | 是 | 操作目标 | `"商家名称"` |
| `targetType` | String | 是 | 目标类型 | `"merchant"` |
| `result` | String | 是 | 操作结果 | `"success"` |
| `details` | Object | 否 | 详细信息 | `{merchantId, oldStatus, newStatus}` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**操作结果说明：**
- `success` - 成功
- `failed` - 失败
- `approved` - 已批准
- `rejected` - 已拒绝

**索引建议：**
- `adminId` + `createdAt` - 复合索引
- `action` + `createdAt` - 复合索引
- `targetType` + `createdAt` - 复合索引

---

### 19. platform_config - 平台配置表

**用途：** 存储平台系统配置参数

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 配置ID（自动生成） | `"config_123456"` |
| `platformFeeRate` | Number | 是 | 平台服务费比例 | `0.08`（8%） |
| `deliveryFee` | Number | 是 | 配送费（分） | `300`（3元） |
| `minOrderAmountLimit` | Number | 是 | 最低订单金额下限（分） | `2000`（20元） |
| `estimatedDeliveryMinutes` | Number | 是 | 预计送达时间（分钟） | `30` |
| `orderTimeoutMinutes` | Number | 是 | 订单超时时间（分钟） | `20` |
| `depositAmount` | Number | 否 | 校园兼职保证金（分），默认 25 元 = 2500 分 | `2500` |
| `subscribeMsgOrderTplId` | String | 否 | 订单状态类订阅消息模板 ID（≤32 字；用于支付/配送等状态推送） | 公众平台申请的模板 ID |
| `subscribeMsgNewOrderTplId` | String | 否 | 商家新订单通知订阅消息模板 ID（商户提醒类目） | 公众平台申请的模板 ID |
| `createdAt` | Date | 是 | 创建时间（系统启动时间） | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**说明：** 此表通常只有一条记录，存储平台的全局配置。`depositAmount` 为校园兼职准入保证金，缴纳满 30 天后可申请退还并退出兼职。`subscribeMsgOrderTplId` 配置后，订单支付成功、商家/骑手状态变更、送达完成时会向用户发送订阅消息（详见《订阅消息配置说明》）。兼容旧字段名 `subscribeMessageOrderStatusTemplateId`。

---

### 20. campus_partners - 校园兼职表

**用途：** 存储用户申请校园兼职、缴纳保证金及退还状态，与 openid 一一对应。

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 记录ID（自动生成） | - |
| `openid` | String | 是 | 用户 openid（唯一） | `"oABC123..."` |
| `status` | String | 是 | 状态：pending_payment 待缴纳 / active 已开通 / refund_requested 已申请退保 / refunded 已退还 | `"active"` |
| `depositPaidAt` | Date | 否 | 保证金缴纳成功时间（用于满 30 天可退校验） | `2025-01-01T00:00:00Z` |
| `refundRequestedAt` | Date | 否 | 用户申请退款时间 | - |
| `createdAt` | Date | 是 | 创建时间 | - |
| `updatedAt` | Date | 是 | 更新时间 | - |

**索引建议：** `openid` 唯一索引

**说明：** 仅 `status === 'active'` 时可接单（任务大厅 + 配送）；缴纳满 30 天后可申请退保证金并退出兼职。可选字段 `depositOrderId` 记录本次开通对应的保证金支付单（deposit_orders._id），退款时用于原路退。

---

### 20.1 deposit_orders - 保证金支付单表

**用途：** 记录校园兼职保证金支付与退款，用于调起微信支付及原路退。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `_id` | String | 是 | 支付单ID（自动生成） |
| `openid` | String | 是 | 用户 openid |
| `amountFen` | Number | 是 | 金额（分） |
| `status` | String | 是 | pending 待支付 / paid 已支付 / refunded 已退 |
| `outTradeNo` | String | 是 | 商户订单号（调起支付用，如 DEP+时间戳） |
| `transactionId` | String | 否 | 微信支付订单号（支付成功后填） |
| `refundedAt` | Date | 否 | 退款完成时间 |
| `createdAt` | Date | 是 | 创建时间 |
| `updatedAt` | Date | 是 | 更新时间 |

**索引建议：** `outTradeNo` 唯一（用于防重复支付）；**不要对 `openid` 建唯一索引**，同一用户可有多条记录（首次缴纳、退款后再次缴纳等）。

---

### 21. system_settings - 系统设置表

**用途：** 存储系统级别的设置（如维护模式）

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 设置ID（自动生成） | `"setting_123456"` |
| `enabled` | Boolean | 是 | 是否启用维护模式 | `false` |
| `message` | String | 否 | 维护提示信息 | `"系统维护中，请稍后再试"` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**说明：** 此表通常只有一条记录，用于控制系统的维护模式

---

## 其他集合

### 22. invite_codes - 邀请码表

**用途：** 存储商家注册邀请码

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 邀请码ID（自动生成） | `"invite_123456"` |
| `code` | String | 是 | 邀请码（唯一） | `"INVITE123"` |
| `maxUses` | Number | 是 | 最大使用次数 | `10` |
| `usedCount` | Number | 是 | 已使用次数 | `5` |
| `status` | String | 是 | 状态（active/inactive/expired） | `"active"` |
| `description` | String | 否 | 描述 | `"商家注册邀请码"` |
| `expiredAt` | Date | 是 | 过期时间 | `2025-12-31T23:59:59Z` |
| `lastUsedAt` | Date | 否 | 最后使用时间 | `2025-01-01T00:00:00Z` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `code` - 唯一索引
- `status` + `expiredAt` - 复合索引

---

### 23. sales_statistics - 销售统计表（可选）

**用途：** 存储销售统计数据（如果使用）

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 统计ID（自动生成） | `"stat_123456"` |
| `storeId` | String | 是 | 店铺ID | `"store_123456"` |
| `date` | Date | 是 | 统计日期 | `2025-01-01` |
| `orderCount` | Number | 是 | 订单数量 | `50` |
| `orderAmount` | Number | 是 | 订单金额（分） | `50000` |
| `createdAt` | Date | 是 | 创建时间 | `2025-01-01T00:00:00Z` |
| `updatedAt` | Date | 是 | 更新时间 | `2025-01-01T00:00:00Z` |

**索引建议：**
- `storeId` + `date` - 复合索引

---

### 24. withdrawals - 商家提现记录表

**用途：** 存储商家提现申请与处理结果，供账户余额统计（仅 `status: 'success'` 计入已提现）及提现记录查询

**字段说明：**

| 字段名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| `_id` | String | 是 | 提现ID（自动生成） | - |
| `merchantId` | String | 是 | 商家ID（关联 merchants._id） | - |
| `storeId` | String | 是 | 店铺ID（关联 stores._id） | - |
| `amount` | Number | 是 | 提现金额（单位：分） | `10000`（100元） |
| `status` | String | 是 | 状态：pending-待审核/打款，success-已打款，failed-打款失败，rejected-已拒绝 | `"pending"` |
| `payType` | String | 否 | 打款方式（wechat/bank，预留） | `"wechat"` |
| `accountInfo` | Object | 否 | 收款信息（预留，如微信 openid、姓名等） | - |
| `remark` | String | 否 | 备注（商家填写或管理员填写） | - |
| `adminRemark` | String | 否 | 管理员处理备注 | - |
| `processedAt` | Date | 否 | 处理完成时间（打款/拒绝时） | - |
| `createdAt` | Date | 是 | 创建时间 | - |
| `updatedAt` | Date | 是 | 更新时间 | - |

**索引建议：**
- `merchantId` + `status` - 复合索引（提现列表、余额统计）
- `merchantId` + `createdAt` - 复合索引（按时间排序列表）

---

## 📊 集合统计

| 分类 | 集合数量 | 集合列表 |
|------|---------|---------|
| 用户相关 | 2 | users, addresses |
| 商家相关 | 2 | merchants, stores |
| 订单相关 | 2 | orders, refunds |
| 商品相关 | 3 | products, categories, idle_products |
| 店铺相关 | 1 | store_photos |
| 内容管理 | 6 | announcements, banners, reviews, chat_messages, chat_list, messages |
| 系统管理 | 5 | admin_accounts, admin_sessions, admin_logs, platform_config, system_settings |
| 其他 | 2 | invite_codes, withdrawals |
| **总计** | **23** | |

---

## 🔑 重要字段说明

### 金额字段
- **金额统一以"分"为单位存储**（如：25元存储为2500）
- 订单相关金额字段：`amountGoods`、`amountDelivery`、`platformFee`、`amountTotal`等
- 商品价格：`price`（分）

### 时间字段
- 所有集合都有 `createdAt` 和 `updatedAt` 字段
- 使用 `db.serverDate()` 创建，自动使用服务器时间
- 日期类型为 `Date` 对象

### 状态字段
- 大多数集合都有 `status` 字段，表示记录的状态
- 常见状态值：`active`、`pending`、`deleted`、`inactive` 等

### ID字段
- `_id`：所有集合的主键，自动生成
- 关联字段：使用其他集合的 `_id` 作为外键
- `openid`：微信用户唯一标识，在多个集合中使用

---

## 📝 注意事项

1. **数据一致性**
   - 删除商家时，需要同时处理关联的店铺、商品等数据
   - 删除订单时，需要考虑退款、评价等关联数据

2. **索引优化**
   - 根据查询频率创建合适的索引
   - 避免创建过多索引影响写入性能

3. **数据安全**
   - 密码字段使用SHA256加密存储
   - 敏感信息不要记录在日志中

4. **数据清理**
   - 定期清理过期的日志数据
   - 清理已删除的记录（软删除改为硬删除）

---

## 📚 相关文档

- [云函数功能文档](./云函数功能文档.md)
- [API接口文档](./API接口文档.md)

---

**文档版本：** v1.0  
**最后更新：** 2025-01-03  
**维护人员：** 开发团队

