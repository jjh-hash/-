# 校园外卖系统 - 云函数权限文档

> 本文档说明每个云函数需要的数据库权限和云函数调用权限  
> 最后更新：2026-04-11

---

## 📋 说明

在微信云开发中，每个云函数都需要配置相应的数据库权限。本文档列出每个云函数需要的权限配置。

**云函数访问数据库的机制：** 云函数在云端运行时，默认拥有**云环境管理员权限**，可读写该环境下已存在的任意集合，无需在控制台为「某个云函数」单独勾选「对某集合的权限」。因此「为某云函数配置对某集合的读写权限」实际只需：**在云开发控制台创建该集合**（若尚未创建），云函数即可访问。

### Web 管理端（admin-web）

- 浏览器端使用 **`@cloudbase/js-sdk`**：需开启云开发 **匿名登录**，并将部署域名加入 **Web 安全域名**。
- 调用方式与小程序管理端一致：云函数入参中带 **`adminSessionToken`**（由 `admin-web/src/cloud.js` 的 `callAdminFunction` 自动合并到请求体），各云函数的鉴权规则与小程序携带会话时相同。
- **轮播图**：Web 端通过 SDK **`uploadFile`** 将图片传到云存储（建议路径前缀 `banners/`），得到 **fileID**（`cloud://...`）后调用 `bannerManage` 的 `create`/`update`，不新增云函数。

---

## 支付相关

### paymentManage - 支付管理

**数据库权限：**
- `orders` 集合：
  - 读取权限：仅限订单所属用户（通过 `userOpenid` 字段验证）
  - 写入权限：无（支付状态更新由 `orderManage` 云函数处理）
- `unified_payments` 集合：读取权限（用于统一付款预下单，校验 openid 与 status）

**云函数调用权限：**
- 所有已登录用户可调用
- 需要验证订单归属权（订单的 `userOpenid` 必须与当前用户 `OPENID` 一致）

**操作说明：**
- `unifiedOrder`: 需要读取订单信息，验证订单归属和支付状态
- `unifiedPrepay`: 统一付款预下单，读取 `unified_payments` 文档（unifiedPaymentId），校验 userOpenid 与 status，调微信统一下单后返回支付参数
- `unifiedOrderDeposit`: 保证金统一下单（不读 DB，由 campusPartnerManage 调用，需传入 openid、totalFee、outTradeNo、description）
- `queryOrder`: 需要读取订单信息，验证订单归属
- `queryOrderByOutTradeNo`: 按商户订单号查询支付结果（不读 DB，用于保证金支付校验）
- `refundOrder`: 订单取消/退款（由 orderManage、refundManage 调用），支持可选 refundAmount（元）用于部分退款，需商户证书
- `refundByOutTradeNo`: 按商户订单号退款（用于保证金原路退，需商户证书）
- `transferToBalance`: 企业付款到零钱（商家提现打款），需商户证书，由 withdrawManage 调用

**配置步骤：**
1. 在云开发控制台 -> 数据库 -> 权限设置
2. 找到 `orders` 集合
3. 设置权限为：仅创建者可读写（或自定义权限，确保用户只能访问自己的订单）

---

## 订单相关

### orderManage - 订单管理

**数据库权限：**
- `orders` 集合：读写权限
- `stores` 集合：读取权限
- `merchants` 集合：读取权限
- `users` 集合：读取权限
- `platform_config` 集合：读取权限
- `campus_partners` 集合：读取权限（接单/抢单前校验校园兼职是否已开通）
- `unified_payments` 集合：读写权限（统一付款：创建预支付单、支付成功后更新状态）

**云函数调用权限：**
- 所有已登录用户可调用
- 部分操作需要验证权限（如商家操作需要验证商家身份；任务接单、配送抢单需校园兼职已开通或骑手审核通过）
- **管理类**：`getAdminOrderList` 需有效 `adminSessionToken`；`completeOrder` 仅允许管理员会话；`cancelOrder` 在用户/商家无权时，可凭管理员会话通过

**操作说明（统一付款）：**
- `createUnifiedOrders`: 入参 address、stores（多店 cart 数据）；为每店创建一笔订单（payStatus: unpaid，unifiedPaymentId 指向新创建的 unified_payments 文档），并创建一条 unified_payments（out_trade_no、orderIds、totalFeeFen、status: pending）；返回 orderIds、totalFeeFen、unifiedPaymentId、out_trade_no
- `updateUnifiedPaymentPaid`: 入参 unifiedPaymentId；校验 userOpenid 后将该条 unified_payments 置为 paid，并将 orderIds 中全部订单更新为 payStatus: 'paid'
- `createOrder` / `createUnifiedOrders` 金额规则：后端按地址楼层计算配送费（1-3楼=1.5元、4-6楼=2元、无法识别默认2元），线上仅收配送费（`amountPayable`）；`amountGoods` 仅用于展示代买金额；骑手收入按楼层计（1-3楼=1元，4-6楼=1.3元）
- 2026-04-06 结构优化：楼层/配送费/骑手收入规则已抽离到 `orderManage/pricingRules.js`，仅代码结构优化，不涉及权限变更
- **校区隔离（2026-04-11）**：`getReceiveOrders` 支持入参 `campus`（`白沙校区` / `金水校区`）；`createExpressOrder` / `createRewardOrder` / `createGamingOrder` 可将客户端传入的 `campus` 写入 `orders.campus`。数据库需在 `orders` 集合中存在 `campus` 字段（可为空的历史单由查询端按白沙规则兼容）。
- **校区隔离（2026-04-11 补充）**：`getAvailableOrders`（外卖待抢单）、`grabOrder`（抢配送单）支持入参 `campus`，与 `getReceiveOrders` 规则一致：金水仅本校单；白沙可看本校显式、无 `campus` 或空的历史单。骑手端应传与首页 `homeCurrentCampus` 一致的校区；`grabOrder` 服务端会校验订单 `campus`，防止跨校区抢单。
- **外卖下单与 users.campus（2026-04-11）**：`createOrder`、`createUnifiedOrders` 要求当前用户在 `users` 表中 `campus` 归一化后为「白沙校区」或「金水校区」，否则返回 403；且用户校区须与店铺 `stores.campus` 规则一致（金水店仅金水用户；白沙用户不可买金水店；白沙用户可买白沙或历史无 `campus` 店铺）。
- **商家端商品/分类（2026-04-11）**：`productManage` 中商品归属 `storeId`（与店铺 `stores.campus` 对应）；`addProduct` 校验 `merchant.openid`、所选 `categories` 必须属于本店 `storeId`；`getProducts` / `getProductDetail` / `updateProduct` / `setProductStatus` 通过 `resolveAuthorizedMerchantStore` 禁止冒用他人 `merchantId` 或仅凭 `storeId` 越权。`categoryManage` 的 `getCategories` / `addCategory` 同步校验 `openid` 与 `storeId` 归属，避免跨店拉分类、录菜。

---

## 用户相关

### loginUser - 用户登录

**入参说明（客户端）：**
- `code`：必填，`wx.login` 换取的登录凭证。
- `campus`：可选，`白沙校区` 或 `金水校区`（及兼容 `baisha` / `jinshui`）。传入且合法时，会写入或更新当前用户在 `users` 表中的 `campus` 字段（与首页 `homeCurrentCampus` 枚举一致）。

**数据库权限：**
- `users` 集合：读写权限

**云函数调用权限：**
- 所有用户可调用（包括未登录用户）

---

### updateUserInfo - 更新用户信息

**数据库权限：**
- `users` 集合：读写权限（仅限当前用户）

**云函数调用权限：**
- 所有已登录用户可调用

---

### addressManage - 地址管理

**数据库权限：**
- `addresses` 集合：读写权限（仅限当前用户）

**云函数调用权限：**
- 所有已登录用户可调用

---

### userManage - 用户管理（管理端）

**数据库权限：**
- `users` 集合：读写权限
- `admin_accounts`、`admin_sessions` 集合：读取权限（校验管理员会话）

**云函数调用权限：**
- 所有 action 需有效 `adminSessionToken`

---

## 商家相关

### merchantRegister - 商家注册

**业务说明：** 同一微信 `openid` 可多次注册，生成多条 `merchants` 记录（登录账号 `account` 仍全局唯一）。

**数据库权限：**
- `merchants` 集合：写入权限
- `users` 集合：读写权限（更新/创建用户角色与 `campus`）
- `invite_codes` 集合：读写权限

**云函数调用权限：**
- 所有用户可调用

**入参说明（校区，2026-04-11）：**
- `campus`：可选，取值 `白沙校区` / `金水校区`（与首页 `homeCurrentCampus` 一致）。未传或非法时服务端按 `白沙校区` 处理。写入 `merchants.campus`、`users.campus`；后续 `storeManage` 创建 `stores` 时据此写入 `stores.campus`，保证店铺仅在对应校区列表/菜品流中体现。

---

### merchantLogin - 商家登录

**入参说明：**
- `loginType`：`account`（账号密码）或 `wx`（微信一键登录）
- 账号密码登录：`account`、`password`
- 微信登录：可选传 `merchantId`。同一 `openid` 下有多条商家时，首次不传 `merchantId` 返回 **code `201`** 及 `merchants` 列表供前端选择；用户选定后再次调用并传入 `merchantId` 完成登录

**返回码：**
- `200`：登录成功
- `201`：微信登录且关联多个商家，需选择账号（`data.merchants`、`data.user`）
- `403` / `404`：非商家、用户或商家不存在等

**数据库权限：**
- `merchants` 集合：读取、更新（更新 `lastLoginAt`）
- `stores` 集合：读取权限
- `users` 集合：读取权限（微信登录校验角色）

**云函数调用权限：**
- 所有用户可调用

---

### merchantManage - 商家管理（管理端）

**数据库权限：**
- `merchants` 集合：读写权限
- `stores` 集合：读写权限
- `admin_accounts`、`admin_sessions` 集合：读取权限（校验会话）

**云函数调用权限：**
- 所有 action 需有效 `adminSessionToken`

---

## 商品相关

### productManage - 商品管理

**数据库权限：**
- `products` 集合：读写权限
- `categories` 集合：读取权限
- `stores` 集合：读取权限（验证商家权限）

**云函数调用权限：**
- 商家用户可调用
- 需要验证商家身份

---

### categoryManage - 商品分类管理

**数据库权限：**
- `categories` 集合：读写权限
- `stores` 集合：读取权限（验证商家权限）

**云函数调用权限：**
- 商家用户可调用
- 需要验证商家身份

---

### idleProductManage - 闲置商品管理

**数据库权限：**
- `idle_products` 集合：读写权限

**云函数调用权限：**
- 所有已登录用户可调用
- 部分操作需要验证商品归属权

**说明（校区，2026-04-11）：** `getList` 支持 `data.campus`（`白沙校区` / `金水校区`）；`publish` 可将 `data.campus` 写入文档，用于跳蚤市场按校区展示。历史无 `campus` 的文档在白沙列表中兼容展示，金水列表仅展示 `campus === 金水校区`。

---

## 店铺相关

### storeManage - 店铺管理

**数据库权限：**
- `stores` 集合：读写权限
- `merchants` 集合：读取权限（验证商家权限）

**云函数调用权限：**
- 商家用户可调用
- 需要验证商家身份

**说明（校区，2026-04-11）：** 新建 `stores` 文档时写入 `campus`（来自当前商家 `merchants.campus`，缺省为 `白沙校区`），与 `getStoreList` / `getProductList` 校区隔离一致。

---

### getStoreList - 获取店铺列表

**数据库权限：**
- `stores` 集合：读取权限

**云函数调用权限：**
- 所有用户可调用（包括未登录用户）

**说明（校区）：** 入参 `campus` 须为 `白沙校区` / `金水校区`（兼容 `baisha` / `jinshui`）；**未传或无法识别时返回空列表**。**金水**仅匹配 `stores.campus === '金水校区'`。**白沙**匹配「`campus` 为 `白沙校区`」或「无 `campus` 字段 / 空串 / null」（兼容历史未写字段的店铺）。新开金水店须在库里写 `campus: '金水校区'`。

---

### getProductList - 首页菜品流

**数据库权限：**
- `products` 集合：读取权限（仅上架且审核通过的商品）
- `stores` 集合：读取权限（店铺名称、头像）
- `merchants` 集合：读取权限（用于筛选已审核商家的店铺）
- `categories` 集合：读取权限（按分类名筛选时查询分类 ID）

**云函数调用权限：**
- 所有用户可调用（包括未登录用户）

**说明：** 返回跨店铺的商品列表，用于首页「今日推荐」菜品流；支持分页、关键词搜索与按分类名筛选（参数 `categoryName`，与 `categories.name` 匹配，仅查营业中且已审核店铺下的分类）。入参 `campus` 须为 `白沙校区` / `金水校区`（兼容 `baisha` / `jinshui`）；**未传或无法识别时返回空列表**。店铺校区规则与 **getStoreList** 一致（金水仅显式金水；白沙含历史无 `campus` 字段的店）。

---

### campusPartnerManage - 校园兼职管理

**数据库权限：**
- `campus_partners` 集合：读写权限（按 openid 查询/新增/更新本人记录）
- `platform_config` 集合：读取权限（获取 depositAmount）
- `deposit_orders` 集合：读写权限（创建保证金支付单、更新支付/退款状态）

**云函数调用权限：**
- 已登录用户可调用（需 openid）
- 会调用 `paymentManage` 的 unifiedOrderDeposit、queryOrderByOutTradeNo、refundByOutTradeNo

**说明：** getStatus、apply、createDepositPayment（创建支付单并返回调起支付参数）、confirmDeposit（传入 depositOrderId 校验支付后开通）、requestRefund（缴纳满 30 天后申请并立即执行原路退款）。保证金金额来自 platform_config；锁定期为缴纳满 30 天可退。

**配置步骤（deposit_orders）：**
1. 打开 [微信公众平台](https://mp.weixin.qq.com/) → 进入你的小程序 → **开发** → **开发管理** → **云开发**，进入云开发控制台。
2. 左侧选择 **数据库**，在集合列表中点击 **「+」新建集合**。
3. 集合名称填写：`deposit_orders`，确定创建。
4. 无需再为 campusPartnerManage 单独配置权限：云函数默认具备管理员权限，创建好 `deposit_orders` 后，campusPartnerManage 即可正常读写该集合。

---

### storePhotoManage - 店铺照片管理

**数据库权限：**
- `store_photos` 集合：读写权限
- `stores` 集合：读取权限（验证商家权限）

**云函数调用权限：**
- 商家用户可调用
- 需要验证商家身份

---

## 内容管理

### announcementManage - 公告管理

**数据库权限：**
- `announcements` 集合：读写权限
- `admin_accounts` 集合：读取权限（验证管理员身份）

**云函数调用权限：**
- 仅管理员可调用
- 需要验证管理员身份

---

### bannerManage - 轮播图管理

**数据库权限：**
- `banners` 集合：读写权限
- `admin_accounts` 集合：读取权限（验证管理员身份）

**云函数调用权限：**
- 仅管理员可调用
- 需要验证管理员身份

**说明（校区）：** 客户端 `getList` 可传 `campus`（`白沙校区` / `金水校区`，兼容 `baisha` / `jinshui`），与 `banners.campus` 精确匹配；管理端创建/更新轮播图时应写入对应 `campus`，避免跨校展示。

---

### reviewManage - 评价管理

**数据库权限：**
- `reviews` 集合：读写权限
- `orders` 集合：读取权限
- `stores` 集合：读取权限

**云函数调用权限：**
- 所有已登录用户可调用
- 部分操作需要验证评价归属权

---

### chatManage - 聊天管理

**数据库权限：**
- `chat_messages` 集合：读写权限
- `chat_list` 集合：读写权限

**云函数调用权限：**
- 所有已登录用户可调用
- 需要验证消息归属权

---

### messageManage - 消息管理

**数据库权限：**
- `messages` 集合：读写权限

**云函数调用权限：**
- 所有已登录用户可调用
- 需要验证消息归属权

---

## 系统管理

### adminManage - 管理员管理

**数据库权限：**
- `admin_accounts` 集合：读写权限
- `admin_sessions` 集合：读写权限（创建/删除会话）

**说明：**
- `adminLogin`：校验用户名密码后创建 `admin_sessions` 记录，返回 `sessionToken`、`expiresAt`；可选返回 `customLoginTicket`（依赖 `@cloudbase/node-sdk` 签发，失败不影响会话登录）。
- `adminLogout` / `verifyAdminSession` / `changePassword` / `getAdminInfo`：须在请求中携带有效 `adminSessionToken`（与 `action`、`data` 同级传入云函数）。

**云函数调用权限：**
- `adminLogin`：无需会话；其余敏感操作需有效管理员会话

---

### adminLogManage - 管理员操作日志

**数据库权限：**
- `admin_logs` 集合：读写权限
- `admin_accounts` 集合：读取权限
- `admin_sessions` 集合：读取权限（校验会话）

**云函数调用权限：**
- 所有 action 需有效 `adminSessionToken`

---

### platformConfig - 平台配置管理

**数据库权限：**
- `platform_config` 集合：读写权限
- `admin_accounts` / `admin_sessions`：读取（`updateConfig` 时校验会话）

**云函数调用权限：**
- 读取：所有用户可调用
- 更新：需有效 `adminSessionToken`

---

### systemManage - 系统管理

**数据库权限：**
- `system_settings` 集合：读写权限
- `admin_accounts`、`admin_sessions` 集合：读取权限（校验会话）

**云函数调用权限：**
- `healthCheck`：可匿名/无需管理员会话（便于探活）
- 其余 action 需有效 `adminSessionToken`

---

### statistics - 统计数据

**数据库权限：**
- `orders` 集合：读取权限
- `stores` 集合：读取权限
- `merchants` 集合：读取权限
- `users` 集合：读取权限
- `admin_accounts`、`admin_sessions` 集合：读取权限（管理端 action 校验会话）

**云函数调用权限：**
- 商家端统计：商家用户可调用
- 管理端统计：`getDashboardStats`、`getAdminOverviewStats`、`getAdminOrderStats`、`getAdminUserStats`、`getAdminMerchantStats`、`getAdminFinanceStats` 需有效 `adminSessionToken`

---

## 其他功能

### withdrawManage - 商家提现管理

**数据库权限：**
- `withdrawals` 集合：读写权限
- `merchants` 集合：读取权限
- `orders` 集合：读取权限（用于计算可提现余额）
- `refunds` 集合：读取权限（用于计算可提现余额）

**云函数调用权限：**
- 商家端：已登录商家可调用（提交提现、查询本人提现记录）；云函数内校验 merchantId/openid 归属

**操作说明：**
- `submitWithdrawal`：校验可提现余额、写入提现记录（status: pending）
- `getWithdrawalList`：按 merchantId 分页查询提现记录

---

### refundManage - 退款管理

**数据库权限：**
- `refunds` 集合：读写权限
- `orders` 集合：读写权限
- `stores` 集合：读取权限
- `admin_accounts`、`admin_sessions`、`admins` 集合：读取权限（管理员判定：会话或历史 openid）

**云函数调用权限：**
- 用户端：所有已登录用户可调用（需要验证订单归属）
- 商家端：商家用户可调用（需要验证店铺归属）
- 管理端：`getRefundDetail` 跨用户查看、`updateRefundStatus` 等需有效管理员会话（或兼容 `admins` 集合 openid）

**操作说明：**
- `createRefund`：骑手未取餐前（pending/confirmed/preparing/ready）用户申请退款时自动通过，调用 paymentManage.refundOrder 执行退款并立即发送订阅消息；骑手取餐后不可申请
- `updateRefundStatus`：商家同意/拒绝时，若拒绝且订单已配送，发送「您的餐已送出，拒绝退款」
- `sendRefundNotificationForCancel`：由 orderManage.cancelOrder 调用，取消订单=退款时发送「退款成功」订阅消息

---

### inviteCodeManage - 邀请码管理

**数据库权限：**
- `invite_codes` 集合：读写权限
- `admin_accounts` 集合：读取权限（验证管理员身份）

**云函数调用权限：**
- 仅管理员可调用
- 需要验证管理员身份

---

## 📝 权限配置建议

### 数据库权限设置原则

1. **最小权限原则**：只授予必要的权限
2. **用户数据隔离**：用户只能访问自己的数据
3. **管理员权限**：管理员可以访问所有数据
4. **商家权限**：商家只能访问自己店铺的数据

### 常见权限配置

- **仅创建者可读写**：适用于用户个人数据（地址、订单等）
- **所有用户可读，仅创建者可写**：适用于公开数据（商品、店铺等）
- **仅管理员可读写**：适用于系统配置、管理数据

---

## ⚠️ 注意事项

1. **安全验证**：即使配置了数据库权限，云函数内部仍需进行权限验证
2. **敏感信息**：支付相关配置（API密钥等）应存储在云函数中，不要暴露给前端
3. **定期审查**：定期检查云函数权限配置，确保符合安全要求

---

**文档版本：** v1.0  
**最后更新：** 2026-04-06  
**维护人员：** 开发团队

